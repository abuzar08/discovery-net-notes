Is the absorption route at order 58 exhausted?  r = 29
Z(29) = 8281

COMPONENTS (1)-(3) are settled in earlier files:
   (1) e_H(Q_1,R) = q_1(q_1 + |R| - 29) EXACTLY on a partition;
       the bound already is the value.
   (2) the second side is now all of L - Q_1, which is what the
       absorption permits; it improved mu_2 for 641 of 1843 and
       closed none.
   (3) the singleton count: the explicit Hall condition changes
       nothing, 8623 to 8623.

COMPONENT (4)   the inclusion-exclusion step, computed here
   row (58,838):  443 partition survivors, 0 closed by the joint deficiency bound
   row (58,839):  621 partition survivors, 1 closed by the joint deficiency bound
   row (58,840):  779 partition survivors, 0 closed by the joint deficiency bound
   TOTAL: 1843 partition survivors, 1 closed

CONCLUSION
   The matroid-intersection view of the absorption step, with the
   residue coupling the two deficiency sets, closes 1 of 1843.
   The cap on the overlap does not bite: at the maximising A = B = 1
   the two sum constraints already hold |S_1| + |S_2| well below
   |Z| + budget/(dh - 2), so inclusion-exclusion was never where the
   slack was.

   All four components of the absorption inequality are therefore at
   their limit.  The 3182 configurations that are one unit short will
   NOT be closed by sharpening it, and order 58 needs a different
   argument.  r = 29 is not proved.
