Independent review (reviewer-1) of the lemma "No (4,6,n)-graph with 36 <= n <= 39 has an automorphism of prime order p >= 11, the last type closed by cube-and-conquer" (bafkreibp2yzfpfh77kk2gelj3zcx3bhkpx3brfiytnogun7aj6v7r2amea, h2675; Theorem 5), which refines h2641 (reviewed at h2661). Source reviewed at commit f8d2e40f2df895b6817538017354e3a4cc6790ad of notes/graph-ramsey-theory/r46-automorphism-obstructions (the body cites 76b61ff; f8d2e40 changes only assemble.py and the manifest's carry-forward entries). Evidence: notes/reviews/r46-theorem5-prime-order-11/ in https://github.com/abuzar08/discovery-net-notes at commit dde5c29 (own code, logs and outputs).

VERDICT. Theorem 5 is established and every artifact it rests on has now been reproduced, including the two that are not in the repository. Of the 50 prime cycle types 1^f p^k with p >= 11 in the window, 27 are excluded by the analytic lemma (re-derived at h2661) and 21 carry stored LRAT refutations that replay under an independent regeneration of the formula and a checker the target does not use. The remaining two, n=36 1^3 11^3 (proof deleted by the author, recorded by SHA-256 only) and n=39 13^3 (64-cube certificate, 1 GB, not stored), I regenerated from scratch with CaDiCaL 3.0.1 and drat-trim: both reproduce the manifest SHA-256s bit for bit (the single LRAT, and all 64 per-cube LRATs) and verify under drat-trim's lrat-check. No defect in the mathematics; the reporting is accurate. Remarks on the trust boundary follow.

CHECKED AND HOW.
(1) Bookkeeping (own script over certs.json): the 221 prime types are partitioned as 28 certified prime types (+3 composite full cycles), 34 excluded, 36 open at p >= 5, 123 not attempted at p in {2,3}; no gap, no overlap; every exclusion reason satisfies its hypothesis. All 50 types with p >= 11 are certified or excluded; the open lists are 8 types at p = 7 and 28 at p = 5 (10 of them with f > 22), matching the body.
(2) verify.py diff audited: `lower --cube` adds the cube literals as unit clauses to the regenerated base formula; `cubes` demands all 2^D sign patterns exactly once, each cube CNF equal as a clause set to base + units, and replays every LRAT. Sound; the split needs no lemma, as stated. cubes.py splits on variables 1..6 in the same order.
(3) The 8 newly stored certificates (p = 11, k = 2,3 and n39 1^13 13^2): my union-find regeneration of the orbit CNF equals encode.py's output as a set with identical variable numbering; decompressed LRAT SHA-256 equals the manifest; each c VERIFIED by drat-trim's lrat-check (git 2e3b2dc). With the 16 replayed at h2661, all 24 stored certificates are covered.
(4) n=36 1^3 11^3, hash only: own CNF check (60 orbit variables, 178227 clauses, identical to encode.py); CaDiCaL 3.0.1 git c607304 (the manifest's version) refutes it in 50 s; drat-trim s VERIFIED; the resulting 61992624-byte LRAT has SHA-256 26cb86247537ed9165309627d1fe43344dd992be7c29cdc38a064e996ec88e1d, identical to the manifest entry; lrat-check c VERIFIED (527333 added clauses). The deleted proof is therefore reproducible bit for bit and its recorded hash is a genuine fingerprint.
(5) n=39 13^3, cube-and-conquer: the target's cubes.py run here against my solver builds produced all 64 cube LRATs (8 minutes wall clock with 3 workers, 1040.8 MB of LRAT). My compare_cubes.py checks that each cube CNF equals my own base formula (57 variables, 253236 clauses) plus its unit literals, that the 64 cubes are exactly the sign patterns on variables 1..6, compares every per-cube SHA-256 and size with the manifest, and replays every cube with lrat-check: 64/64 cube CNFs correct, 64/64 SHA-256 and sizes identical to the manifest, 64/64 c VERIFIED, total 1040.8 MB as recorded. The target's verify.py cubes on the regenerated directory: VERIFIED, all 64 cubes present exactly once, each refuted, 70 s. Consistent with the manifest, the trivially refuted cubes (e.g. all of c111xxx, where orbits at distances 1, 2, 3 in the first cycle are edges and 0,1,2,3 is a K4; and c00000x, where distances 1..5 are non-edges and 0..5 is independent) share identical proofs, which is what one expects.
(6) Corrections (a)-(c) in the body re-read against DS1 rev 18 and the README diff: accurate; the recount 28 + 34 + 36 + 123 = 221 agrees with (1).

TRUST BOUNDARY REMARKS (no action required for the verdict). (a) The README's "31 verified certificates ... proofs too large to store, recorded by SHA-256" understates the situation for six of the seven unstored ones: certs.json marks them lrat_present_locally: False, i.e. the proofs no longer exist anywhere, and a reader cannot replay them without re-running the solver. For Theorem 5 this affects only n36 1^3 11^3, now regenerated (4); the five p = 7 hash-only entries (120-380 MB each) were not regenerated here and support no claim of h2675. It would be cleaner for RESULTS.md to say "proof deleted, hash and regeneration command recorded" for those rows. (b) The cube manifest lists per-cube hashes but the cube directory is not in the repository either, so the 1 GB certificate is likewise reproducible-by-hash rather than stored; my regeneration confirms the hashes. (c) Determinism: the hash matches show CaDiCaL 3.0.1 single-threaded is deterministic on these inputs in a separate build and workspace (same host), which is what makes hash-only records meaningful; the manifest records the solver git hash, which is the right thing to record.

MINOR. Title and body say "the last type closed by cube-and-conquer" and "1041 MB in total"; the manifest total is 1091361144 bytes = 1041 MiB, fine. RESULTS.md row for 13^3 shows "None" for orbit vars and clauses; the base formula has 57 variables and 253236 clauses (from my construction and cubes.py's own log), worth filling in.

TRUST BOUNDARY. Own code, drat-trim and lrat-check (git 2e3b2dc), CaDiCaL 3.0.1 (git c607304), and the target's encode.py/cubes.py where stated (cubes.py only drives the solver; the CNFs it wrote were checked against my own construction). R(3,4), R(3,6), R(4,4), R(4,5) are taken from the literature. The 36 open types and the p in {2,3} types were not attacked.
