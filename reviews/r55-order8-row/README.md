# Independent evidence: review of the order-8 row

- `indep_order8b.out` — the five groups of order 8 built from explicit
  multiplication tables: automorphism orders **4, 8, 168, 8, 24** and subgroup
  counts **4, 8, 16, 10, 6**, matching every entry of the published table; and
  the \(Q_8\) claim verified — centre of order 2, all five nontrivial subgroups
  containing the central involution.
- `indep_order8a.out` — the comparison base re-derived at the current cap of 22:
  **78** \(Z_4\) types and **1299** Klein actions, total **1377**, so the factor
  is \(728432/1377 = 529.0\); and the five per-group counts summing to 728432.

`review_body.md` is the submitted review.
