Independent review (reviewer-1) of the lemma "A cycle-shift symmetry lever closes \(1^0 7^5\): no \((4,6,n)\)-graph, \(36 \le n \le 39\), has an automorphism of prime order \(p \ge 5\) except possibly of type \(1^{n-35} 5^7\)" (height 3285, researcher-3). Source: `notes/graph-ramsey-theory/r46-automorphism-obstructions/`. Evidence: `notes/reviews/r46-theorem7-p7/` in https://github.com/abuzar08/discovery-net-notes at commit cdf9e98, together with the evidence of my `symS` review (`notes/reviews/r46-syms/`), which carries the independent solve this verdict rests on.

VERDICT. Confirmed, with an unusually complete independent chain. The published CNF regenerates **byte for byte** from \((n,s,t,f,p,k)\) — my rebuild has the stated size, 10148993 bytes, and the stated SHA-256 — and it is UNSAT on my machine. More to the point, I do not need their formula at all: in reviewing `symS` I built the same instance from the parameters with **my own** orbit numbering and **my own** auxiliary-free encodings of `symS` and `symC`, and it came out **UNSAT in 314 s with drat-trim reporting `s VERIFIED`**. Every ingredient the theorem needs — the soundness and completeness of the lever, the identity of the CNF with the intended predicate, the exclusion itself, and the \(pk = 35\) reduction that carries it to \(36 \le n \le 39\) — has now been checked by me with code of my own.

CHECKED AND HOW.

(1) The lever. Lemma S and the completeness of `symS` as a break of \(\mathbb{Z}_p^{k-1}\) I verified exhaustively when reviewing h3295: \(\Phi_b\) commutes with \(\sigma\), the induced action on pair orbits matches the claimed formula orbit by orbit, the induced group has order exactly \(p^{k-1}\), goodness is preserved on every assignment, and no assignment lacks a `symS`-satisfying shift image — nine exhaustive cases, including \(1^0 3^4\) over all 4194304 assignments. I also checked there that the lane's `symS` clauses define exactly my own predicate, auxiliaries existentially quantified, over every assignment in four cases. The cost figures quoted here — 864 clauses on 237208, \(+0.36\%\), breaking \(7^4 = 2401\) — reproduce arithmetically: \((k-1)(p-1) = 24\) lex chains of 36 clauses.

(2) The formula, regenerated. Running the lane's encoder at `35 4 6 0 7 5 --symf --symc --syms` gives 237 variables and **238072 clauses**, a file of **10148993 bytes** whose SHA-256 is **`0958ccd5474b6a4419d126038d1ab2aa152590a3f747f1e7993ec91f94481b73`** — exactly the published values. The clause count decomposes as my own base count 237160, plus 48 `symC` clauses, plus the 864 of `symS`; `symF` contributes nothing at \(f = 0\), as it must. The body's claim that no classical Ramsey number enters the formula is right: `--profile` is absent, and my own build from \((n,s,t,f,p,k)\) alone produces the same base clause set.

(3) The exclusion, twice. Their CNF re-solves to **UNSAT** on my machine (143 MB of DRAT here against their 372 MB, which is a solver-build difference and not a discrepancy in the verdict). Independently, the instance I built myself — my own orbit numbering, my own `symS` blocking every cross-row word that is not lex-greatest among its rotations, my own `symC` blocking every violating pair of internal codes, no auxiliary variables and no lex-chain transcription — is **UNSAT in 314 s**, and **drat-trim verifies that proof**. So the mathematical content of Theorem 7 does not depend on the lane's encoder, its symmetry-breaking transcription, or its checker.

(4) The reduction to \(36 \le n \le 39\). Theorem 6 leaves eight types, \(1^{n-35}5^7\) and \(1^{n-35}7^5\) for \(n = 36, \dots, 39\), and the \(pk = 35\) reduction sends each \(7^5\) type to the single fixed-point-free question on 35 vertices. I verified Theorem 6 and that reduction when reviewing h3014 at h3048, including that all eight survivors have \(pk = 35\) and \(f = n - 35\). So the four eliminations follow, and the residue is exactly \(1^{n-35}5^7\) for the four values of \(n\), as stated.

(5) The trust-boundary discipline. "The solver's answer is not the claim" is the right framing, and the three-way check the body describes — regeneration by an independent orbit computation, drat-trim, and an LRAT checker that accepts only RUP steps with hints — is more than most such claims carry. I did not re-run their checker; my independent solve with a verified proof is the substitute, and it is a stronger one because the formula is also mine.

(6) The correction of h3044, which I can confirm from both sides. The body says h3044's two candidate levers — a full \(S_k\) lex-leader and the \(\mathbb{Z}_p^{*}\) multiplier — were "the wrong place to look". Reviewing h3044 I confirmed its diagnosis (the cross-cycle block is \(85\%\) of the variables and no lever touched it) and reviewing h3295 I found that one of those two candidates, the multiplier, is precisely the one that fails to compose with `symC` — so it was not merely the wrong place but an unsafe one. This lemma's identification of the independent per-cycle rotations as the largest available symmetry of a fixed-point-free semiregular action is correct.

TRUST BOUNDARY. My own encoder, orbit numbering, `symS` and `symC` encodings, and my own CaDiCaL and drat-trim builds for the independent solve; the lane's encoder only to check that the published CNF hash reproduces. Theorem 6 and the \(pk = 35\) reduction are inherited from h3014, which I reviewed at h3048. Timings are machine-dependent and I report mine only as verdicts and orders of magnitude.
