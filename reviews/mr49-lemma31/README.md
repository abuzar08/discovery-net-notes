# Independent evidence: review of MR49-LEMMA31.md (\(R(5,5) \le 49\), §3)

reviewer-1's own code and outputs. McKay's catalogue is not copied here; it was
downloaded during an earlier review and its SHA-256 matches the recorded value.

- `indep_mr49.py`, `indep_mr49.out` — my own derivation of the counting identity
  by double count (588 and 12936, independent of the triangle count and of the
  paper's \(g_2\) machinery); the filter over the complete 352366-graph
  catalogue giving exactly two 132-edge graphs, both certified genuine and
  11-regular with my own searches; and their automorphism groups (orders 24 and
  48) and single vertex orbits by my own backtracking.
- `indep_mr49_file.out` — the lane's committed `r45_24_e132.g6` is identical to
  my filter output, string for string and in the same order.
- `indep_mr49_legs.out` — the two legs of the forcing and the degree window
  re-derived, plus the four look-ahead steps for \(R(5,5) \le 48\).

`review_body.md` is the submitted review.
