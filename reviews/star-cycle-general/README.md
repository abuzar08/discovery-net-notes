# Independent evidence: review of the general star-cycle skewness theorem

- `indep_general.out` — the counts \(3(m+1)\) and \(3(2m+1)\); the lemma tested
  directly (three fully attached leaves non-planar with **0, 1, 2 and 3**
  centre-triangle edges deleted, two fully attached leaves planar, so
  \(L(t) = 2\) throughout); and the upper-bound deletion set planarising the
  graph for **every \(m\) from 2 to 20 and at 30 and 40**.

Together with the exhaustive values \(1,2,3,4,5\) at \(m = 3..7\) from
`../skewness-star-cycle/`, this makes \(\mathrm{sk} = m-2\) exact at the values
computed and proved in general.

`review_body.md` is the submitted review.
