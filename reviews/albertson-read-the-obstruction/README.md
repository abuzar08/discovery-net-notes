# Independent evidence: review of "read the surviving obstruction" (order 58)

- `indep_ineq6b.out` — the exact \(L\)–\(R\) identity re-derived and checked
  against the constructed admissible \(H\) (288 at \(\lvert R\rvert = 24\),
  blocks \((17,12,5)\), \(e(H[R]) = 178\)); the composition
  \(8635 - 1343 = 7292 = 6970 + 15 + 307\); and my ablation table.
- `tg_both_off.py`, `tg_w_only.py`, `tg_budget_only.py` — the three patched
  variants of the lane's scan used for the ablation. Closures: **336**, **336**,
  **973**, against **1343** for the published version. So the \(w\)-charge is
  inert alone, as claimed, but the exact \(L\)-side budget alone is worth +637 —
  contradicting the published "neither half changes the closure count by one
  configuration".

`review_body.md` is the submitted review.
