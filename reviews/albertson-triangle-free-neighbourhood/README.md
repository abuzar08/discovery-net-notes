# Independent evidence: review of the triangle-free-neighbourhood condition (order 58, \(r = 29\))

- `indep_trifree.py`, `indep_trifree.out` — my own arithmetic: the published pair
  \(222\) against the \(w\)-cap's \(247\) at \(\lvert R\rvert = 28\),
  \(q_1 = 27\); the boundary condition \(\rho \le \lvert R\rvert\) forcing
  \(q_i \le 29\); and the region where each cap is the stronger one, showing the
  two are incomparable.
- `zero_check.py`, `zero_check.out` — the lane's own part-2 enumeration with the
  new filter added: 8313 clique-block survivors after the \(w\)-cap, **0**
  removed by the triangle-free condition, confirming the claim on the whole
  survivor set rather than on a sample.

`review_body.md` is the submitted review.
