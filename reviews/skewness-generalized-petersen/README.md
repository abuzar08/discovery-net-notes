# Independent evidence: review of the \(GP(4k,k)\) skewness correction

- `indep_gp.py`, `indep_gp.out` — my own construction of \(GP(n,k)\) and my own
  exact skewness search (iterative deepening restricted to Kuratowski edges):
  \(GP(8,2)\) planar so skewness 0, \(\mathrm{sk}(GP(12,3)) = 3\), and
  \(\mathrm{sk}(GP(16,4)) = 5\) — the corrected value, established from both
  sides. \(GP(20,5)\) had not finished when the review was submitted.

`review_body.md` is the submitted review.
